[build]
  base = "."
  command = "pnpm install && pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
